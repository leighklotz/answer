# Tool

````bash
    🦶$ ask what branches are not merged into main | tools git
    🤖 git branch --no-merged main

    The following branches have not been merged into `main`:

    * `infer-function`
    * `tool-attempts`
    🦶$ 
````

````bash
    $🦶$ ask what branches are not merged into main | tools git
    🤖 git branch --no-merged main

    There is currently one branch that has not been merged into `main`:

    * `more-tools`
    $🦶$
````


